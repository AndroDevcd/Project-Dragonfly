mod main;

import std.io;
import platform;
import gpio;

def main(args: string[])
{
   load_library("libs/dragonfly");
   setup();
   setup_device();

   for(i := 0; i < 10; i++) {
     println("i am ${get_distance()}cm away");
     thread.sleep(1000);
   }
}
