mod main;

import gpio;

native def get_distance(trg: var, echo: var): var;

const ECHO := 5;
const TRG := 6;

def get_distance() := get_distance(TRG, ECHO);

def setup_device() {
  pin_mode(TRG, OUTPUT);
  pin_mode(ECHO, INPUT);
}
